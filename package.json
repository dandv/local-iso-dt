{
  "name": "local-iso-dt",
  "version": "3.0.3",
  "description": "Convert the most common types of dates into ISO8601/RFC3339-formatted YYYY-MM-DDTHH:MM:SS strings in the local timezone",
  "keywords": [
    "datetime",
    "date",
    "time",
    "format",
    "string",
    "convert",
    "local",
    "timezone",
    "ISO8601",
    "RFC3339"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/dandv/local-iso-dt.git"
  },
  "license": "MIT",
  "author": {
    "name": "Dan Dascalescu",
    "url": "https://github.com/dandv"
  },
  "type": "commonjs",
  "main": "index.js",
  "files": [
    "index.ts",
    "index.mjs",
    "index.js"
  ],
  "scripts": {
    "clean": "rm index*.js index.mjs",
    "lint": "eslint index*.ts",
    "prepublish:cjs": "tsc index.ts --esModuleInterop --removeComments",
    "prepublish:esm": "tsc index.ts -t ES2015 --types node && mv index.js index.mjs",
    "prepublish": "npm run prepublish:esm; npm run prepublish:cjs",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^26.0.13",
    "@typescript-eslint/eslint-plugin": "^4.0.1",
    "@typescript-eslint/parser": "^4.0.1",
    "eslint": "^7.8.1",
    "eslint-plugin-jest": "^24.0.0",
    "jest": "^26.4.2",
    "ts-jest": "^26.3.0",
    "typescript": "^4.0.2"
  },
  "comments": {
    "scripts": {
      "prepublish:esm": "TypeScript can't generate .mjs output directly (https://github.com/microsoft/TypeScript/issues/18442) and --outFile doesn't work with ES Modules"
    }
  }
}
